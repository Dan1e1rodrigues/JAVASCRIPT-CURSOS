// Declaramos qual tag chamariamos, button.
const btn = document.querySelector('button');

// Declaramos o evento que ocorreria com a interação do botão.
btn.addEventListener('click', function(){
    // Para que assim a mensagem fosse exibida.
    alert("Você apertou o botão");
});